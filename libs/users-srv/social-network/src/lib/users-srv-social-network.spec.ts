import { usersSrvSocialNetwork } from './users-srv-social-network';

describe('usersSrvSocialNetwork', () => {
  it('should work', () => {
    expect(usersSrvSocialNetwork()).toEqual('users-srv-social-network');
  });
});
