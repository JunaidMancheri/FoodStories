import { apiGatewayComment } from './api-gateway-comment';

describe('apiGatewayComment', () => {
  it('should work', () => {
    expect(apiGatewayComment()).toEqual('api-gateway-comment');
  });
});
