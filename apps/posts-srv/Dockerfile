# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build posts-srv`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t posts-srv`.
FROM docker.io/node:lts-alpine


WORKDIR /app

RUN addgroup --system posts-srv && \
          adduser --system -G posts-srv posts-srv

COPY dist/apps/posts-srv posts-srv
RUN chown -R posts-srv:posts-srv .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix posts-srv --omit=dev -f install

CMD [ "node", "posts-srv" ]
