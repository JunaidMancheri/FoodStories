# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build users-srv`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t users-srv`.
FROM docker.io/node:lts-alpine

WORKDIR /app

RUN addgroup --system users-srv && \
          adduser --system -G users-srv users-srv

COPY dist/apps/users-srv users-srv
RUN chown -R users-srv:users-srv .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix users-srv --omit=dev -f install

CMD [ "node", "users-srv" ]
